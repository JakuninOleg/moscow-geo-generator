{
  "name": "moscow-geo-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint:js": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "lint": "yarn lint:js"
  },
  "dependencies": {
    "@amcharts/amcharts4": "^4.10.2",
    "nuxt": "^2.14.0",
    "random-points-on-polygon": "^0.0.4",
    "vue-google-charts": "^0.3.3",
    "vue-yandex-maps": "^0.10.9"
  }
}
